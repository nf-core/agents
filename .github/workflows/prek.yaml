name: Lint with prek

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: j178/prek-action@v3
